apiVersion: v1
kind: Service
metadata:
  name: ${DATABASE_SERVICE_NAME}-master
  labels:
    app: ${DATABASE_SERVICE_NAME}-master
    role: master
spec:
  ports:
    - name: redis-master
      protocol: TCP
      port: 6379
      targetPort: 6379
  selector:
    app: ${DATABASE_SERVICE_NAME}-master
    role: master
  type: ClusterIP
  sessionAffinity: None
status:
  loadBalancer: {}
